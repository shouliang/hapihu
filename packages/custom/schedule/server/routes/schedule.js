(function() {
    'use strict';

    /* jshint -W098 */
    // The Package is past automatically as first parameter
    module.exports = function(Schedule, app, auth, database, circles) {

        var requiresAdmin = auth.requiresAdmin;
        var requiresLogin = auth.requiresLogin;

    };
})();
